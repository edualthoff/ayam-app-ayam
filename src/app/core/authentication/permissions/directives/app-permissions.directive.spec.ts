import { AppPermissionsDirective } from './app-permissions.directive';

describe('AppPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
