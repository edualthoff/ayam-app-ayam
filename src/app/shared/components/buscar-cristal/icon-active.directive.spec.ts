import { IconActiveDirective } from './icon-active.directive';

describe('IconActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IconActiveDirective();
    expect(directive).toBeTruthy();
  });
});
