import { MaskIonInputDirective } from './mask-ion-input.directive';

describe('MaskIonInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskIonInputDirective();
    expect(directive).toBeTruthy();
  });
});
